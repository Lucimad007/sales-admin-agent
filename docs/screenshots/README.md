# Screenshots

Capture these after `pnpm dev` and a local login as `leo.a@example.org`:

1. `login.png` — sign-in panel
2. `dashboard.png` — KPIs and recent sales
3. `customers.png` — searchable table
4. `pipeline.png` — Kanban columns
5. `tally.png` — right rail (setup empty state without an API key, or a genUI card with a key)

Store the files in this folder for the take-home submission.
